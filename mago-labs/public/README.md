# Public Assets Folder

Upload your static files here (such as `burhan-founder.png`).

Any file placed in this `public` folder will be served at the root of the website (e.g., `/burhan-founder.png`). During production builds, Vite will copy everything from this folder into the production build output (`dist/`) automatically.
